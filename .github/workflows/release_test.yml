name: RELEASE TEST
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-2022
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Step 1
        shell: cmd
        run: |
          set "PATH=%PATH%;C:\MORE"
          echo "PATH=%PATH%" >> %GITHUB_%ENV%
      - name: Step 2
        shell: cmd
        run: |
          echo %PATH%

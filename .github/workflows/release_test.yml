name: RELEASE TEST
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-2022
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Prepare
        shell: cmd
        run: |
          echo BLAH > blah.txt
          7z e check.7z
          dir
      - uses: actions/checkout@v6
      - name: Release
        run: |
          dir
          gh release create "tag" -d check.7z
